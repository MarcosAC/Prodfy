<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:control="clr-namespace:Prodfy.Controls"
             x:Class="Prodfy.Views.IdentificacaoView"
             BackgroundImage="splash_bg4.png">
    
    <NavigationPage.TitleView>
        <control:CustomTitleView  Titulo="Identificação" Icone2="&#xf029;" />
    </NavigationPage.TitleView>

    <ContentPage.Content>
        <StackLayout Style="{StaticResource baseStyleStackLayout}">
            <Image Source="qrcode.jpg">
                <Image.GestureRecognizers  >
                    <TapGestureRecognizer Tapped="OnTapped_LeitorQR" />
                </Image.GestureRecognizers>
            </Image>

            <Label Style="{StaticResource styleBaseLabel}" 
                   Text="Clique no botão e posicione o código da etiqueta dentro do limite indicado!" 
                   FontSize="Large" />

            <StackLayout>
                <Label x:Name="LblCodigoQR" 
                       Text="{Binding DadosCodigo}" 
                       HorizontalOptions="CenterAndExpand" />
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>