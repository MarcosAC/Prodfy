<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:Prodfy.Controls"
             x:Class="Prodfy.Views.LoginView"
             BackgroundImage="splash_bg2.png">
    <ContentPage.Content>          
        <StackLayout Style="{StaticResource baseStyleStackLayout}">
            <Image Source="prodfy_logo_white.png" />
            
            <Label Style="{StaticResource styleLabelTitulo}" 
                   Text="DISPOSITIVO:"
                   Margin="0,30,0,0" />

            <Label Style="{StaticResource styleLabelTexto}"
                   Text="50000" />

            <Label Style="{StaticResource styleLabelTitulo}"
                   Text="USUÁRIO:" />

            <Label Style="{StaticResource styleLabelTexto}"
                   Text="ROMULO BALGA" />

            <Label Style="{StaticResource styleLabelTitulo}"
                   Text="SENHA:" />

            <!--<Frame CornerRadius="5"
                   BorderColor="Gray"
                   BackgroundColor="Transparent"
                   Padding="2"
                   HeightRequest="50"
                   Margin="0,0,0,15">
                
                <Entry Placeholder="senha de acesso"
                       HorizontalTextAlignment="Center"
                       BackgroundColor="White" 
                       PlaceholderColor="LightGray" />
            </Frame>-->
            <Entry Placeholder="senha de acesso"
                   PlaceholderColor="LightGray"
                   HorizontalTextAlignment="Center"
                   BackgroundColor="White" 
                   Margin="0,0,0,15" />

            <Button Style="{StaticResource styleButton}" 
                    Text="Entrar" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>